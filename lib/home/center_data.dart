import 'package:flutter/material.dart';
import 'package:sandbox_level5/home/widget_data.dart';

class CenterData extends StatelessWidget {
  const CenterData({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: WidgetData(),
    );
  }
}
